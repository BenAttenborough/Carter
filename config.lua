require "libs.Colorized-for-Love.colorize"

local config = {}

config.playfield = {
	top = 0,
	right = 800,
	bottom = 600,
	left = 0
}

config.playerStartingPos = {
	x = 15,
	y = 15
}

config.backgroundColour = hex("#AAA")

return config
